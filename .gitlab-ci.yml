stages:
  - build
  - production


build_images_for_k8s:
  stage: build
  only:
    - master
  script:
    -  ./build/run-build
  tags:
    - production

deploy_prod_k8s:
  stage: production
  image: lwolf/helm-kubectl-docker:latest
  only:
    - master
  script:
    - ./build/run-k8s
  tags:
    - production
